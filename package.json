{
  "name": "jira-task-copy-to-clipboard",
  "version": "0.0.1",
  "description": "Add a copy button beside jira task titles to copy the task id and task title",
  "scripts": {
    "dev": "webextension-toolbox dev",
    "build": "webextension-toolbox build"
  },
  "devDependencies": {
    "webextension-toolbox": "latest"
  }
}
